# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/

# Runtime worker state (per-workspace audit/sqlite/artifacts)
.state/

# Secrets (never commit key material)
*.key
secrets.key

# OS
.DS_Store

# Runtime worker ledger data (created under SWORKER_HOME during runs)
company/
